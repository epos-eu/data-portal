# YAML config file
url: "${URL}"
verbose: false
reporter: "tap:no-skip"
"block-domain": "google-analytics.com"
# we can put comments here, yay!
asserts:
  requests: 50
  redirects: 0
  domains: 5
  timeToFirstByte: 600
  httpTrafficCompleted: 8000